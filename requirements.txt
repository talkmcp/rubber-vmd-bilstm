# ────────────────────────────────────────────────────────────────────
# requirements.txt
#
# Python 3.11 (tested). Should also work with 3.10 / 3.12.
# Install with:   pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────────

# --- core deep-learning stack ---
torch>=2.0,<3.0
numpy>=1.24,<2.0
pandas>=2.0,<3.0

# --- VMD decomposition ---
vmdpy>=0.2                 # Variational Mode Decomposition (Dragomiretskiy & Zosso, 2014)

# --- classical baselines & statistics ---
statsmodels>=0.14          # ARIMA, ADF unit-root test, Diebold-Mariano (future)
scipy>=1.10,<2.0
scikit-learn>=1.3,<2.0     # MinMaxScaler, metrics

# --- plotting (used by analysis scripts; not required for training) ---
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# --- utility ---
tqdm>=4.65                 # progress bars
pyyaml>=6.0                # config files
joblib>=1.3                # parallel multi-seed runs
